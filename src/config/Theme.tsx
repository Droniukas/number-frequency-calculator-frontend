import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#f1f3f6",
      main: "#027799",
      dark: "#30373c",
      contrastText: "#fff",
    },
  },
});

export default theme;
